# 日晷 · 一卷山河

免费古代线抽卡与图鉴小游戏，横屏游玩。当前是可运行的角色样卡试玩版，不含剧情、配音、战斗、付费或账号系统。

## 试玩内容

单抽、十连、跳过揭晓、自动升星、图鉴筛选、指定卡牌兑换、每日签赠、最近300次获得记录，以及本地存档导入导出均已实现。设置可调整音效、音量、快速展卷、稀有卡强调和减少动态效果。

95张卡牌条目共用5张AI生成角色插画，并非95张独立插画。画风为水墨与二次元结合。角色名暂用官职；世界观为唐风架空，不宣称服饰完全符合史实。提示词与版本记录位于 `art-direction/`，原图另保留在完整本地交付的 `art-direction/selected/`，不随网页上传。网页使用压缩后的本地图片，不依赖第三方图片服务或字体服务。

## 启动与验证

需要 Node.js 20 或更高版本，无需安装依赖。在项目目录运行：

```text
node scripts/serve.mjs
```

浏览器打开 `http://127.0.0.1:4173`。请通过本地服务或网站访问，不要直接双击HTML文件；浏览器通常会拦截本地文件中的模块加载。

```text
node --test tests/*.test.mjs
node scripts/build.mjs
node scripts/simulate.mjs 1000
```

构建结果在 `dist/`。游戏只读取当前浏览器的本地存档，不上传玩家数据。GitHub Pages与其他托管平台仍可能记录常规访问日志。

## GitHub Pages 发布

本项目根目录就是可直接托管的静态网站。仓库上传后，在仓库 Settings → Pages 中选择：

1. Source：Deploy from a branch。
2. Branch：main，目录：/(root)。
3. 保存，等待GitHub部署完成，以设置页显示的地址为准。

仓库预期地址为 `https://summer-eve.github.io/SummerEve/`，启用并部署成功前该地址可能无法访问。代码自动测试与网页部署是两件事。GitHub Pages对中国大陆的访问质量受网络条件影响，不能保证人人稳定打开；后续可将相同的 `dist/` 内容部署到国内可访问的静态托管服务。

## itch.io 网页版发布

将 `dist/` 内的文件打包为ZIP，确保ZIP根目录直接包含 `index.html`，不能多套一层文件夹。创建HTML项目，上传ZIP并选择在浏览器中运行。建议展示尺寸1280×720，启用全屏选项，说明推荐横屏。网页ZIP不是Windows安装程序；本版尚未制作Windows安装包。

## 规则摘要

- 基础概率：SSR 1.5%、SR 3.5%、R 25%、N 70%，不含保底。
- SSR最多60抽必出，没有软保底。抽取SSR重置计数，兑换不重置。
- SR按账号累计抽数的固定十抽分组保底，单抽和十连共用。SSR不替代SR；若组末命中SSR且本组没有SR，额外补发SR，不额外收费或发印记。
- 每抽1印记，100印记兑换任意未满星SSR的一份。
- 满星卡仍在池中，同稀有度内权重由100降至5。满星重复卡转为余晖，N/R/SR/SSR分别为1/3/8/20。兑换N/R/SR一份分别消耗10/30/80余晖。
- 初始60晷签，每个领取日50；累计第3/7/14天额外40/80/120。累计总量第1天110、第3天250、第7天530、第14天1000。领取中断不清零。此后每日50直到全部满星。
- 全部满星为95卷、790份有效卡牌，不只是每卷各一份。

## 存档与限制

游戏存档保存在浏览器的 `rigui.save.v1`，另留上一份有效备份。导出JSON可迁移到其他设备或网址；导入会替换当前进度。清理浏览器数据会丢失进度，请定期导出。损坏存档不会自动覆盖，恢复页允许下载原始数据。

多窗口写入通过浏览器Web Locks串行处理，并监听存档变化。建议在现代Chrome、Edge、Firefox或Safari中游玩；缺少Web Locks的旧浏览器请仅打开一个游戏标签页。日期依据设备本地日期，仅阻止重复领取与日期倒退，不提供防作弊服务。

## 2026-09-20 验证记录

21项自动测试通过，覆盖保底、权重、兑换、奖励、存档与多随机种子规则校验。Windows Edge无界面浏览器检查通过：领取、十连、跳过、刷新续玩、筛选、详情、设置、导出导入、双窗口写入、横屏/竖屏提示和损坏存档恢复。截图见 `qa/`。尚未在真实iPhone、安卓设备或不同地区网络上验收。

1000组确定性种子模拟中，全部在累计领取第13–14天完成满星，中位数14天。模拟每天用完晷签，优先兑换持有份数少的SSR，余晖保留到第14天，再按SR、R、N顺序补缺。该结果只代表指定策略与样本，不保证所有玩家14天完成，也不能视为所有兑换策略的结果。当前送抽偏宽裕，暂不擅自削减。

后续重点：真实设备体验、国内访问方案、95张独立卡面制作范围，以及用户确认后的平衡调整。
