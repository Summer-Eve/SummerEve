import {CARDS} from './data.js';
import {pityCopy,starsMarkup} from './presentation.js';
const copy=pityCopy(41);
const examples=[['SSR',2,'黄色'],['SR',3,'紫色'],['R',4,'蓝色'],['N',6,'绿色']];
document.querySelector('#review').innerHTML=`<section class="review-pull"><div class="pull-area"><div class="pity"><span class="ssr-pity">${copy.ssr}</span><span class="sr-pity">${copy.sr}</span></div><div class="pull-buttons"><button class="pull single" type="button">展卷一次<small>晷签 × 1</small></button><button class="pull primary" type="button">展卷十次<small>晷签 × 10</small></button></div><small class="hint">${copy.shared}</small></div></section><section class="review-cards">${examples.map(([rarity,n,color],i)=>{const c=CARDS.find(c=>c.rarity===rarity&&c.characterIndex===i);return `<article><h3>${rarity} · ${color}</h3><div class="card ${rarity}"><img src="${c.art}" alt="${rarity}星级样例"><span class="rarity">${rarity}</span><div class="card-name"><small>星级效果示例</small><strong>${c.title}</strong>${starsMarkup(rarity,n)}</div></div><div class="review-legend"><small>未获得</small>${starsMarkup(rarity,0)}</div><div class="review-legend"><small>满星</small>${starsMarkup(rarity,c.max)}</div></article>`;}).join('')}</section><p class="review-foot">星星由左至右逐颗点亮。SSR共3星、SR共5星、R共8星、N共10星；空心星表示尚未获得。这里只调整显示，保底、概率、星级上限与存档结构不变。上方按钮仅为样式展示，不执行抽取。</p>`;
